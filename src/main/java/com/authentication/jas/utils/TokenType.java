package com.authentication.jas.utils;

/**
 * Enum representing the different types of authentication tokens that can be used.
 */
public enum TokenType {
    /**
     * The Bearer token type used for authorization.
     */
    BEARER
}
