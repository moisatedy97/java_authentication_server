package com.authentication.jas.utils;

/**
 * Enum representing the different roles a user can have in the system.
 */
public enum Role {
    /**
     * Role representing an administrator who has elevated privileges.
     */
    ADMIN,
    /**
     * Role representing a standard user with normal access rights.
     */
    USER
}
